# הוראות התקנה — לקלוד, לא לארי

> אתה מתקין מאמן טריאתלון אישי על המכונה הזאת. המשתמש הוא **ארי
> אוריאלי**, טריאתלט, **לא מתכנת**. דבר איתו בעברית, אל תבקש ממנו
> להריץ פקודות, ואל תסביר שלבים טכניים — תעשה ותדווח בסוף.

---

## 🔴 שלושה קווים אדומים

1. **לעולם אל תבקש ממנו את הסיסמה לגרמין ואל תקליד אותה בשבילו.**
   ההתקנה פותחת פרומפט — **הוא מקליד שם, לא בצ'אט.** אם הוא מדביק לך
   סיסמה, אמור שלא תשתמש בה והפנה אותו לפרומפט.
2. **אל תדחוף שום אימון לשעון שלו בזמן ההתקנה.** ההתקנה קריאה בלבד.
3. **אל תדרוס קבצים קיימים.** גבה לפני כל שינוי.

---

## ההתקנה — פקודה אחת

```
uv run python install.py
```

זה עושה הכל: תלויות · בדיקות · התחברות לגרמין (הוא מקליד) · משיכת
היסטוריה · בניית דשבורד. **בערך 5 דקות**, רובן משיכת ההיסטוריה.
בטוח להריץ שוב — כל שלב בודק אם כבר נעשה.

⚠️ אם `uv` לא מותקן, הסקריפט יעצור ויגיד לך בדיוק מה להריץ.

**בסוף הוא מדפיס שני דברים שנשארו, עם הנתיבים כבר ממולאים:**

**א. התוסף** — תגיד לארי לגרור את `ari-coach.dxt` לחלון של Claude
Desktop. הוא יישאל "תיקיית ההתקנה" — **תן לו את הנתיב שהסקריפט הדפיס.**
ואז שיסגור ויפתח מחדש את Claude Desktop.

🔴 **ל-Claude Desktop יש בעיה שנמדדה: הוא כותב מחדש את
`claude_desktop_config.json` בזמן שהוא רץ ומוחק עריכות חיצוניות.**
לכן **אל תערוך את הקובץ הזה.** התוסף עוקף את זה לגמרי. אם התוסף לא
נתמך בגרסה שלו — שיסגור לגמרי את האפליקציה (⌘Q), ורק אז מזג את
`config/claude_desktop_config.ari.json` לתוך `mcpServers`, ואז שיפתח.

**ב. הוראות המאמן** — התוכן של `config/coach-instructions.md` נכנס
ל-Project Instructions. **תראה לו איפה ותן לו להדביק.** זה המסמך
שהופך קלוד גנרי למאמן שלו.

---

## אחרי ההתקנה — האונבורדינג

תפתח איתו שיחה ותשאל, **שאלה-שתיים בכל פעם:**

- מתי בדיוק המרוץ באילת, ומה הזמן שהוא רוצה לשבור
- **השיא הכללי שלו** — הוא מהיר מהשיא באילת
- כמה שעות בשבוע ואילו ימים · בריכה או ים · טריינר או כביש ·
  פציעות · הענף הכי חלש

ואז `save_athlete_profile`. **בלי זה אין ספירה לאחור ואין פאנל מרוץ** —
הדשבורד יגיד "עדיין לא הגדרת יעד", וזה תקין.

---

## אחרי כל אימון — זה מה שהוא רוצה ממך

1. ‏`refresh_from_garmin` עם `days=2`
2. ‏**`analyze_last_session`** — כלי אחד שמחזיר סיכום, **הקפות**,
   אזורי דופק, אזורי הספק, ומה שהיה מתוכנן. ‏`which` = `last` /
   `run` / `bike` / `swim`, או תאריך.
3. ‏**תסתכל על פיזור ההקפות, לא על הממוצע.** בשחייה האחרונה שלו
   החזרות היו ‏1:32 → ‏1:48 → ‏2:15 ל-100מ'. דעיכה כזאת אומרת משהו
   אחר לגמרי מקצב אחיד.

⚠️ **אין נתונים תוך כדי אימון.** גרמין לא משדרת בזמן אמת: שעון ←
טלפון ← ‏Garmin Connect ← רענון, דקות אחרי הסיום. **אל תבטיח "לייב".**

---

## הדשבורד המקומי

הסקריפט מדפיס את הפקודה. נפתח על `127.0.0.1` בלבד. משם: סימון
בוצע/דולג · הזזת יום · **דחיפה לשעון** · רענון.
**הוא מסנכרן את עצמו** אם המטמון ישן מיותר משעתיים — נמדד: העמוד
נחסם **0ms** והסנכרון רץ 6 שניות ברקע.

---

## מה כבר ידוע עליו — אל תשאל שוב

| | |
|---|---|
| שני מרוצים ב-2026 | ‏1:16:18 · ‏**1:13:34** |
| **פיצול השיא** | שחייה 13:07 · ‏**T1 4:39** · אופניים 32:31 · ‏T2 1:17 · ריצה 21:58 |
| עוגנים | ‏FTP ‏~209W · ‏CSS ‏~91 שנ'/100מ' · ‏VO2max ‏51.8 |
| דופק מרבי לפי ענף | ריצה **194** · שחייה **173** · אופניים **163** |

🔴 **ושני דברים שהמדידה חשפה, ושווים יותר מכל עצה:**

1. ‏**ה-2:44 שהוא "שיפר" בין המרוצים הוא כמעט כולו מסלול קצר יותר.**
   מנורמל, המהירות שלו כמעט לא זזה. **על מסלול תקני השיא שלו הוא
   ‏1:15:25, לא 1:13.**
2. ‏**‏T1 שלו לקח 4:39 — ‏6% מהמרוץ.** מעבר מתורגל הוא 1:30–2:00.
   ‏**התיקון הזה לבדו שווה יותר מכל מה שהוא צריך למצוא.**

⚠️ **אל תשווה בין מרוצים בזמן גולמי.** יש `race.py` עם נרמול מרחק —
תשתמש בו. אני כמעט דיווחתי שקר בלעדיו.

---

## אימות סופי

הסקריפט מדפיס כמה תובנות ומדדים נבנו. אם יש פחות תובנות ממה שציפית —
**זה תקין**: לכל כלל יש מינימום דגימות, והוא שותק מתחתיו במקום להמציא.
תדווח לו בעברית, בשלוש שורות: מה עובד, מה נשאר, ומה הצעד הבא.
